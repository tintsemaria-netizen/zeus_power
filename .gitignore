node_modules/
dist/
out/
*.tsbuildinfo

# Capture may contain demo tokens/session ids — keep out of version control.
input/
*.zip

# Local env
.env
.env.local

# NEVER commit the PAT / any key material
GitKey
*.pem
*.key
*ghp_*
